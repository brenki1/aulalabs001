#include <stdio.h>

int main (){
    int x, y, z, w;
    

    printf("insira um valor inteiro de 4 dígitos: ");
    scanf("%d%d%d%d", &x, &y, &z, &w);
    printf("%d\n%d\n%d\n%d\n", x, y, z, w);
    
    

    return 0;
}